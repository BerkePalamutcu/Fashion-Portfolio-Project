import Navbar from '../Navbar';
import Carousel from '../Carousel';
import Slider from '../Slider';
const Home = () => {
  return (
    <div>
      <Navbar />
      <Slider />
    </div>
  );
};

export default Home;
