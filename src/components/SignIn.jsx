import styled from 'styled-components';

const SignInContainer = styled.div`
  positition: relative;
`;

const SignIn = () => {
  return <SignInContainer>SignIn</SignInContainer>;
};

export default SignIn;
