import styled from 'styled-components';
import { NavbarContainer } from '../Navbar/navbar.styles';
export const StaticNavbarContainer = styled(NavbarContainer)`
  position: static;
  box-shadow: 0 2px #e0e0eb;
  transition: ease-in-out 1s;
`;
